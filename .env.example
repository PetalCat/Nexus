# Path to the SQLite database file
DATABASE_URL=./nexus.db

# All service connections (Jellyfin, Overseerr, Sonarr, etc.) are configured
# through the Settings UI and stored in the database — no env vars needed.
